// ─── JS OS Global Config ───
// Change app names here — they propagate everywhere automatically.
window.JSOS_CONFIG = {
    os:   'JS OS',
    chat: 'JS Chat',
    ai:   'JS AI',
    tube: 'JS Tube',
    call: 'JS Call',
};
